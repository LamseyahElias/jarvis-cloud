{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install hermes-agent pyyaml"
  },
  "deploy": {
    "startCommand": "bash entrypoint.sh",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "healthcheckPath": "/health",
    "healthcheckTimeout": 30
  }
}